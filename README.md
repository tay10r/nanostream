nanostream
==========

This is an example of an image compression algorithm.
It's not particularly great at fidelity, but it does compress at a fixed rate of 0.5 bits per pixel (RGB).
It's also very simple.
